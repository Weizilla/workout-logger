language: java
jdk: oraclejdk8
sudo: false
before_install:
  - npm install jspm
  - jspm config registries.github.auth "$jspmGithubToken"
after_success:
  - mvn clean test jacoco:report coveralls:report
