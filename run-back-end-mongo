if [[ -n $@ ]]; then
    mvn $@
    if [[ $? -ne 0 ]]; then
        exit
    fi
fi

mvn -pl server-app spring-boot:run -Drun.profiles=dev,mongo -Drun.jvmArguments="-Xdebug -Xrunjdwp:transport=dt_socket,server=y,suspend=n,address=8001"
